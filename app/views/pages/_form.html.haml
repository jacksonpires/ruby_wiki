= form_for @page, url: @page.new_record? ? pages_path : page_path(@page.id) do |f|
  = f.hidden_field :slug

  .field
    = f.label :title
    = f.text_field :title

  .field
    = f.label :body
    = f.text_area :body

  .actions
    = f.submit 'Save'
